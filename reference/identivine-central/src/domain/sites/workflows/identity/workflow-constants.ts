/* eslint-disable prettier/prettier */

// 35 hours (to give time for retries, etc to drain)
export const STANDARD_TOTAL_REVERIFICATION_TIME_IN_SECONDS = (60 * 60 * 24 * 35);

// 11:30 hours (to give time for retries, etc to drain)
export const PLUS_TOTAL_REVERIFICATION_TIME_IN_SECONDS = (60 * 60 * 24 * 11) + (30 * 60);

// 3:40 hours (to give time for retries, etc to drain)
export const PROFESSIONAL_TOTAL_REVERIFICATION_TIME_IN_SECONDS = (60 * 60 * 24 * 3) + (40 * 60);
